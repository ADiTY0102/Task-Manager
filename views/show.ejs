<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <title><%= filename %> - Task Details</title>
</head>
<body class="bg-gradient-to-br from-zinc-950 via-zinc-900 to-zinc-950 text-zinc-100 font-sans antialiased">
    <%- include('toast') %>
    <div class="min-h-screen max-w-4xl mx-auto px-6 py-12">
        
        <nav class="mb-10">
            <a class="text-blue-400 hover:text-blue-300 text-sm font-medium transition-colors flex items-center gap-1 group" href="/">
                <span class="transform group-hover:-translate-x-1 transition-transform">←</span> Back to Dashboard
            </a>
        </nav>

        <article class="bg-zinc-900 border border-zinc-800 rounded-2xl shadow-2xl overflow-hidden">
            <div class="px-8 py-10 border-b border-zinc-800 bg-zinc-900/50">
                <div class="flex items-center gap-3 mb-4">
                    <span class="px-3 py-1 text-xs font-semibold bg-blue-600/10 text-blue-400 border border-blue-600/20 rounded-full uppercase tracking-wider">
                        Task View
                    </span>
                </div>
                <h1 class="text-4xl md:text-5xl font-extrabold text-white tracking-tight leading-tight">
                    <%= filename %>
                </h1>
            </div>

            <div class="px-8 py-12">
                <div class="prose prose-invert max-w-none">
                    <p class="text-zinc-300 text-lg md:text-xl leading-relaxed whitespace-pre-wrap">
                        <%= filedata %>
                    </p>
                </div>
            </div>

            <div class="px-8 py-6 bg-zinc-950/50 border-t border-zinc-800 flex justify-between items-center">
                <p class="text-zinc-500 text-sm">
                    Viewing raw text file
                </p>
                <div class="flex gap-4">
                    <a href="/edit/<%= filename %>" class="px-5 py-2 bg-zinc-800 hover:bg-zinc-700 text-white text-sm font-medium rounded-lg transition-all border border-zinc-700">
                        Edit Task
                    </a>
                </div>
            </div>
        </article>

        <footer class="mt-12 text-center">
            <p class="text-zinc-600 text-xs">
                Task Manager &bull; 2026 Build
            </p>
        </footer>
    </div>
</body>
<script src="/public/javascripts/script.js"></script>
</html>