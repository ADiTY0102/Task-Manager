<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <link rel="icon" type="image/x-icon" href="./images/icon-head.png">
    <title>Task Manager App</title>
    <style>
        /* Smooth transition for hover effects */
        .task-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        .task-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.3);
        }
    </style>
</head>
<!-- Chnagig to test the ci/cd pipeline working -->
<body class="bg-zinc-950 text-zinc-100 font-sans antialiased">
    <div class="min-h-screen max-w-6xl mx-auto px-6 py-12">
        
        <header class="mb-12">
            <h1 class="text-4xl font-bold tracking-tight text-white mb-2">Task Manager</h1>
            <p class="text-zinc-400">Organize your workflow and stay productive.</p>
        </header>

        <div class="bg-zinc-900 border border-zinc-800 p-8 rounded-xl shadow-xl mb-12">
            <form action="/create" method="post" class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-zinc-400 mb-2">Task Title</label>
                    <input class="block w-full rounded-lg outline-none px-4 py-3 bg-zinc-800 border border-zinc-700 focus:border-blue-500 focus:ring-1 focus:ring-blue-500 transition-all text-white" 
                        type="text" placeholder="What needs to be done?" name="title" required>
                </div>
                <div>
                    <label class="block text-sm font-medium text-zinc-400 mb-2">Details</label>
                    <textarea class="block w-full rounded-lg outline-none px-4 py-3 bg-zinc-800 border border-zinc-700 focus:border-blue-500 focus:ring-1 focus:ring-blue-500 transition-all text-white resize-none h-32"
                        placeholder="Add some context or notes..." name="details"></textarea>
                </div>
                <button type="submit" class="w-full md:w-auto px-8 py-3 bg-blue-600 hover:bg-blue-500 text-white font-semibold rounded-lg transition-colors shadow-lg shadow-blue-900/20">
                    Create Task
                </button>
            </form>
        </div>

        <hr class="border-zinc-800 mb-12">

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% if(files.length > 0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task-card flex flex-col justify-between p-6 rounded-xl bg-zinc-900 border border-zinc-800">
                        <div>
                            <div class="flex items-start justify-between mb-4">
                                <h2 class="text-xl font-bold text-white truncate pr-4" title="<%= val %>">
                                    <%= val %>
                                </h2>
                                <span class="px-2 py-1 text-xs font-medium bg-zinc-800 text-zinc-400 rounded">Task</span>
                            </div>
                        </div>
                        
                        <div class="flex items-center justify-between mt-6 pt-4 border-t border-zinc-800">
                            <a class="text-blue-400 hover:text-blue-300 font-medium text-sm transition-colors" href="/files/<%= val %>">
                                View Details →
                            </a>
                            <a class="text-zinc-500 hover:text-zinc-300 text-sm transition-colors flex items-center gap-1" href="/edit/<%= val %>">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                                </svg>
                                Edit
                            </a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-span-full py-20 text-center">
                    <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-zinc-900 border border-zinc-800 mb-4">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-zinc-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
                        </svg>
                    </div>
                    <h3 class="text-zinc-400 text-lg font-medium">No tasks found!</h3>
                    <p class="text-zinc-600 text-sm">Fill out the form above to get started.</p>
                </div>
            <% } %>
        </div>
    </div>

    <script src="/javascripts/script.js"></script>
</body>

</html>